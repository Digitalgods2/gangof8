# Codex verified memory

- Do not emit wrapper scripts that depend on files absent from the declared working set.
- For binary deliverables, the build command and exact produced filename are part of the implementation.
