# Claude verified memory

- Recovery must change a relevant variable and run the same acceptance gate again.
- A build producer must be self-contained or declare and receive every immutable input it imports.
