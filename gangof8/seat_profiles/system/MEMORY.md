# System coordinator verified memory

- A recorded failure is not recovery. Recoverable failures must drive another changed action.
- Never accept a generator as a requested binary deliverable; run and validate its build.
- Never retry an unchanged owner, prompt, model route, and inputs after the same fault.
